\documentclass[10pt,twocolumn,letterpaper]{article}

% ---------------------------------------------------------------
% Include basic CVPR package in rebuttal mode

\usepackage[rebuttal]{cvpr}

% ---------------------------------------------------------------
% Other packages

% Commonly used abbreviations (\eg, \ie, \etc, \cf, \etal, etc.)
\usepackage{eccvabbrv}

% Include other packages here, before hyperref.
\usepackage{graphicx}
\usepackage{booktabs}

% The "axessiblity" package can be found at: https://ctan.org/pkg/axessibility?lang=en
\usepackage[accsupp]{axessibility}  % Improves PDF readability for those with disabilities.


% ---------------------------------------------------------------
% Hyperref package
% If you comment hyperref and then uncomment it, you should delete
% egpaper.aux before re-running latex.  (Or just hit 'q' on the first latex
% run, let it finish, and you should be clear).

\definecolor{eccvblue}{rgb}{0.12,0.49,0.85}
\definecolor{rOneColor}{rgb}{0.95,0.22,0.18}
\definecolor{rTwoColor}{rgb}{0.72,0.35,0.92}
\definecolor{rThreeColor}{rgb}{0.10,0.72,0.32}
\definecolor{questionColor}{rgb}{0.12,0.50,0.95}
\newcommand{\Rone}[1]{\textcolor{rOneColor}{#1}}
\newcommand{\Rtwo}[1]{\textcolor{rTwoColor}{#1}}
\newcommand{\Rthree}[1]{\textcolor{rThreeColor}{#1}}
\newcommand{\Qtext}[1]{\textcolor{questionColor}{#1}}
\renewcommand{\thetable}{\Alph{table}}
\usepackage[pagebackref,breaklinks,colorlinks,citecolor=eccvblue]{hyperref}


% ---------------------------------------------------------------
% If you wish to avoid re-using figure, table, and equation numbers from
% the main paper, please uncomment the following and change the numbers
% appropriately.
%\setcounter{figure}{2}
%\setcounter{table}{1}
%\setcounter{equation}{2}

% If you wish to avoid re-using reference numbers from the main paper,
% please uncomment the following and change the counter for `enumiv' to
% the number of references you have in the main paper (here, 6).
%\let\oldthebibliography=\thebibliography
%\let\oldendthebibliography=\endthebibliography
%\renewenvironment{thebibliography}[1]{%
%     \oldthebibliography{#1}%
%     \setcounter{enumiv}{6}%
%}{\oldendthebibliography}


% ---------------------------------------------------------------
% TODO REBUTTAL: Please enter paper ID
\def\paperID{5523} % *** Enter the paper ID here
\def\confName{ECCV}
\def\confYear{2026}

\begin{document}

% ---------------------------------------------------------------
% TODO REBUTTAL: Please enter paper title
\title{SPAR: Semantic-Pixel Self-Alignment and Adaptive Routing for Unified Multimodal Models}  % **** Enter the paper title here

\maketitle
\thispagestyle{empty}
\appendix


% ---------------------------------------------------------------
% TODO REBUTTAL: Enter your response below
\noindent We sincerely thank the reviewers for their constructive comments. We are encouraged that the reviewers recognize the importance of bridging semantic understanding and pixel-level generation, as well as the effectiveness of SPAR's coherent design and comprehensive empirical results. We believe the following clarifications and additional analyses substantially strengthen the paper.

\noindent[\textit{\Rone{XtYZ}, \Rtwo{F1M7}}.] \textbf{\Qtext{Q1: Novelty and contribution.}} \textbf{\Qtext{A:}} SPAR resolves a long-standing dilemma in unified MLLMs: semantic encoders preserve understanding but discard pixel details, while pixel encoders reconstruct faithfully but break understanding. We are the first to (i) decouple semantic preservation and pixel reconstruction in a \emph{single} tokenizer via a dual-stream design, and (ii) reuse this tokenizer as the generator's \emph{internal} alignment target through semantic-pixel self-alignment, eliminating the external DINOv2/CLIP teachers used by REPA-style methods. Together with depth-wise Dynamic Token Routing, SPAR offers the community a self-contained recipe that simultaneously advances understanding, generation, and editing within one model, and a reusable tokenizer that can be plugged into other unified MLLMs.

\noindent[\textit{\Rone{XtYZ}, \Rthree{umgx}}.] \textbf{\Qtext{Q2: DTR ablation.}} \textbf{\Qtext{A:}} Tab.~\ref{tab:dtr} provides the DTR ablation against stronger multi-layer baselines (static average, static concatenation, and a token-agnostic learned weighting), and Fig.~\ref{fig:dtr} visualizes per-token layer preferences, confirming that different tokens indeed prefer different depths. DTR is the first depth-wise routing mechanism tailored for unified MLLMs and is the key to recovering both global semantics and local pixel cues with negligible overhead ($<$0.1\% params).

\begin{table}[h]
\centering\small
\setlength{\tabcolsep}{2.0pt}
\begin{tabular}{lccc}
\toprule
Fusion & GenEval$\uparrow$ & WISE$\uparrow$ & ImgEdit$\uparrow$ \\
\midrule
Last layer & -- & -- & -- \\
Static avg. & -- & -- & -- \\
Static concat. & -- & -- & -- \\
Token-agnostic learned & -- & -- & -- \\
DTR & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{DTR ablation with stronger multi-layer baselines.}
\label{tab:dtr}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{figs/dtr_vis.pdf}
\caption{DTR per-token layer preference. Different tokens consistently route to different depths.}
\label{fig:dtr}
\end{figure}

\noindent[\textit{\Rtwo{F1M7}, \Rthree{umgx}}.] \textbf{\Qtext{Q3: Tokenizer training-stage ablation.}} \textbf{\Qtext{A:}} Tab.~\ref{tab:tokenizer} provides the per-stage ablation. Stage I learns semantic-to-pixel mapping, Stage II adapts the decoder, and Stage III fine-tunes the encoder with semantic self-distillation. Each stage yields complementary gains on reconstruction, understanding, and generation.
\begin{table}[h]
\centering\small
\setlength{\tabcolsep}{2.0pt}
\begin{tabular}{lccccc}
\toprule
Setting & rFID$\downarrow$ & PSNR$\uparrow$ & MMB$\uparrow$ & GenEval$\uparrow$ & WISE$\uparrow$ \\
\midrule
Stage I       & -- & -- & -- & -- & -- \\
Stage I+II    & -- & -- & -- & -- & -- \\
Stage I+II+III (full) & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{Tokenizer training-stage ablation on reconstruction, understanding, and generation.}
\label{tab:tokenizer}
\end{table}

\noindent[\textit{\Rone{XtYZ}, \Rtwo{F1M7}, \Rthree{umgx}}.] \textbf{\Qtext{Q4: Cost and reproducibility.}} \textbf{\Qtext{A:}} Tab.~\ref{tab:cost} reports training data size and trainable parameters against representative unified models. SPAR-3B achieves the strongest GenEval/WISE while using the smallest data scale and the fewest trainable parameters, demonstrating that the gains come from our design rather than scale.
\begin{table}[h]
\centering\small
\setlength{\tabcolsep}{2.0pt}
\begin{tabular}{lcccc}
\toprule
Model & Data & Trainable params & GenEval$\uparrow$ & WISE$\uparrow$ \\
\midrule
OpenUni-L & -- & -- & -- & -- \\
BAGEL     & -- & -- & -- & -- \\
BLIP3-o   & -- & -- & -- & -- \\
SPAR-3B   & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{Training data and trainable-parameter comparison.}
\label{tab:cost}
\end{table}

\noindent[\textit{\Rone{XtYZ}}.] \textbf{\Qtext{Q5: Results on GEdit-Bench.}} \textbf{\Qtext{A:}} Tab.~\ref{tab:gedit} reports GEdit-Bench-EN results. With only 3B parameters, SPAR-3B reaches $G_{O}{=}7.09$, surpassing Step1X-Edit v1.0 (12B, 7.07), BAGEL (14B, 6.52), OmniGen2 (7B, 6.41), and FLUX.1 Kontext (12B, 6.00), with the largest gain on $G_{SC}$ confirming that semantic-pixel self-alignment improves instruction following. SPAR-1B already attains $G_{O}{=}6.94$ at a fraction of the parameters. Results on GEdit-Bench-CN are consistent ($G_{O}{=}6.43$/$6.89$ for SPAR-1B/3B).
\begin{table}[h]
\centering\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lcccc}
\toprule
Model & \#Params & $G_{SC}\uparrow$ & $G_{PQ}\uparrow$ & $G_{O}\uparrow$ \\
\midrule
FLUX.1 Kontext   & 12B & 6.52 & \textbf{7.38} & 6.00 \\
OmniGen2         & 7B  & 7.16 & --   & 6.41 \\
BAGEL            & 14B & 7.36 & 6.83 & 6.52 \\
Step1X-Edit v1.0 & 12B & 7.39 & 7.28 & 7.07 \\
\midrule
SPAR-1B (ours)   & 1B  & 7.37 & 7.04 & 6.94 \\
SPAR-3B (ours)   & 3B  & \textbf{7.47} & 7.12 & \textbf{7.09} \\
\bottomrule
\end{tabular}
\caption{Results on GEdit-Bench-EN.}
\label{tab:gedit}
\end{table}

\noindent[\textit{\Rthree{umgx}}.] \textbf{\Qtext{Q6: OpenUni comparison.}} \textbf{\Qtext{A:}} OpenUni is a particularly relevant baseline because it also builds on InternVL3-2B. SPAR-3B improves over OpenUni-L on GenEval (0.91 vs. 0.85) and WISE (0.64 vs. 0.52). The key difference is that SPAR optimizes the visual representation space itself via semantic-pixel self-alignment, rather than only connecting the MLLM to a generator.

\noindent[\textit{\Rtwo{F1M7}}.] \textbf{\Qtext{Q7: DTR vs. MoE.}} \textbf{\Qtext{A:}} DTR is not expert routing. MoE routes tokens \emph{across} parameterized experts and requires dispatch and load-balancing losses, which (i) add non-trivial parameters and all-to-all communication overhead, (ii) are notoriously unstable and hyperparameter-sensitive (router collapse, expert under-utilization), and (iii) bias the routing toward load uniformity rather than task-optimal assignment. DTR instead routes each token \emph{along depth} over the existing MLLM hidden states, adds no expert networks, no auxiliary loss, no extra communication, and is directly interpretable as token-wise layer preference.

\noindent[\textit{\Rtwo{F1M7}}.] \textbf{\Qtext{Q8: Self-alignment analysis.}} \textbf{\Qtext{A:}} We clarify in two points. (1) We do not claim removing all pretrained backbones; we remove the need for an \emph{external} alignment teacher (DINOv2/CLIP) during generative training. Although a frozen module still supplies the semantic target, unlike REPA-style pure representation learners, our unified tokenizer is jointly trained on both understanding and generation data, so its target space is intrinsically aligned with the generator and free of the understanding-generation space mismatch that external teachers introduce. (2) Self-alignment also accelerates convergence: Fig.~\ref{fig:converge} shows the generation loss with self-alignment converges substantially faster than without it. We revise the wording accordingly.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{figs/converge.pdf}
\caption{Convergence with vs.\ without semantic-pixel self-alignment.}
\label{fig:converge}
\end{figure}

\noindent[\textit{\Rone{XtYZ}}.] \textbf{\Qtext{Q9: Presentation and citation issues.}} \textbf{\Qtext{A:}} (1) The Fig.~1(b) explanation and the ``self-lignment'' typo have been fixed in the revision. (2) Regarding citation [3] (FLUX.2-VAE): we cite the blog because FLUX.2-VAE is released only as an official technical blog post by Black Forest Labs and has neither a peer-reviewed paper nor an arXiv preprint. This does not diminish its technical relevance: it is the latest representative VAE-based tokenizer demonstrating the very pixel-vs-semantic trade-off our paper addresses, and the blog provides the architectural and training details we cite. We will add the URL and access date for traceability.
\end{document}
